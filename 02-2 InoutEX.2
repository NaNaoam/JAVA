import java.util.Scanner;
public class inoutEX{

publix static void main (String[] args) {

Scanner sc = new Scanner(System.in);

		System.out.print("문자?");

			String c = sc.next();

			System.out.println(c+" "+c.length());

			System.out.print("문자?");

			c = sc.next();				//String c 라고 적지 않기 중복 지역 선언 오류가 생김 쓸거라면 c 변수만 적기

			System.out.println(c+" "+c.length());

			sc.nextLine();		
//키보드 버퍼를 읽은 후 비운다. 엔터를 반복해서 입력할 때 이름 값을 칠 기회를 만들어준다 만약 스페이스 바로 입력했다면 굳이 필요 없는 작업이다.
// next() 는 개행문자를 무시하고 입력을 받고 공백을 기준으로 한 단어 또는 한 문자씩 입력받음.
//nextLine()은 한줄 단위로 입력을 받기 때문에 (공백문자, 개행문자인 분리자)개행문자로 포함한다.
//버퍼에 입력된 문자열을 개행 문자까지 다 가져온다.
			System.out.print("이음");

			c = sc.nextLine();		

			System.out.println(c+" "+c.length());
			

			sc.close();
			

			
	}
}











/*
import java.util.Scanner;

public class ScannerEX {
	//Scanner sc=new Scanner(System.in); 맴버변수로 선언 후 사용 후에도 닫으면 안되고 제일 마지막에 닫아줘야함
	// 스캐너는 한개 불러서 한개만 사용
	public static void main(String[] args) {
		/*Scanner sc;
		sc=new Scanner(System.in);
		
		System.out.println("이름, 도시, 나이, 체중, 독신여부 입력");
		String name, city;
		int age;
		double wgt;
		boolean sg;
		name=sc.next();
		city=sc.next();
		age=sc.nextInt();
		wgt=sc.nextDouble();
		sg=sc.nextBoolean();
		
		System.out.print("이름은 "+name+" 사는 곳은 "+city);
		System.out.println(" 나이는 "+age+"세 ");
		System.out.print("몸무게는 "+wgt+" 독신인가? "+sg);
		
		*/
		
		Scanner sc=new Scanner(System.in);
		 System.out.print("입력Test?");
		 String a=sc.next();
		 System.out.println(a+" "+a.length());
		 System.out.print("입력Test?");
		 String b =sc.next();
		 System.out.println(b+" "+b.length());
		 sc.nextLine(); //키보드 버퍼를 모두 읽어 비운다ㅏㅏㅏㅏㅏㅏㅏㅏㅏ. / 중요요요요요요
		 System.out.print("이름?");
		 String c =sc.nextLine();//nextLine은 띄어쓰기 포함해서 읽음.  nextLine이랑 next 의 차이점 알것.
		 // nextLime은 개행을 읽어버린다 -> 버퍼 비우기//
		 System.out.println(c+" "+c.length());
		 //스캐너를 닫고 또 열면 오류
		 
		 System.out.print("문자?");//next는 string
		 char d= sc.next().charAt(0);
		 System.out.println(d+" ");
	}

}





*/
